# --- Core orchestration ---
langgraph>=0.2.0
langchain>=0.3.0
langchain-community>=0.3.0

# --- Embeddings & rerank (BGE-M3) ---
FlagEmbedding>=1.2.10
sentence-transformers>=3.0.0

# --- Vector store: pgvector (primary) ---
psycopg[binary]>=3.1
pgvector>=0.3.0
sqlalchemy>=2.0

# --- Sparse / keyword retrieval ---
rank-bm25>=0.2.2

# --- Local inference ---
ollama>=0.3.0
# llama-cpp-python>=0.2.90   # uncomment if using GGUF directly instead of Ollama

# --- Ingestion / OCR (Arabic RTL + layout) ---
pytesseract>=0.3.10
pdf2image>=1.17.0
pymupdf>=1.24.0
Pillow>=10.0.0
langdetect>=1.0.9
# surya-ocr>=0.4.0           # optional: stronger RTL layout OCR

# --- Evaluation ---
ragas>=0.2.0
datasets>=2.20.0

# --- Profiling / utils ---
pynvml>=11.5.0
python-dotenv>=1.0.0
pyyaml>=6.0
tqdm>=4.66.0
pandas>=2.2.0
pytest>=8.0.0
